# ============================================================
# face-to-bmi — Unified Python Dependencies (W1 + W2)
# ============================================================
# Tested on Python 3.10 (Google Colab default).
# Install:  pip install -r requirements.txt
#
# Notes:
# - Versions chosen for compatibility with Colab + DeepFace + PyTorch 2.x
# - If you hit conflicts, post in the team chat before changing versions
# ============================================================

# --- Core scientific stack ---
numpy>=1.24,<2.0
pandas>=2.0,<2.3
scipy>=1.10
scikit-learn>=1.6,<1.8         # Beste's saved joblibs are pickled with sklearn 1.6.1 — widen range so loading works
joblib>=1.3

# --- Deep learning ---
torch>=2.1,<2.5
torchvision>=0.16,<0.20

# --- Face recognition / feature extraction ---
deepface>=0.0.93           # VGG-Face & Facenet512 feature extractors
facenet-pytorch>=2.5.3     # MTCNN face detection / alignment

# --- Image I/O ---
Pillow>=10.0
opencv-python>=4.8

# --- Visualization & dim reduction ---
matplotlib>=3.7
seaborn>=0.13
umap-learn>=0.5.4

# --- Utilities ---
tqdm>=4.66

# --- Web app / API ---
streamlit>=1.30

# --- Notebook runtime (Colab already provides these; pin for local) ---
ipykernel>=6.27
jupyter>=1.0
